name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      VITE_LIVEBLOCKS_AUTH_ENDPOINT: ${{ secrets.VITE_LIVEBLOCKS_AUTH_ENDPOINT }}
      VITE_LIVEBLOCKS_PUBLIC_KEY: ${{ secrets.VITE_LIVEBLOCKS_PUBLIC_KEY }}
      VITE_TELEMETRY_ENDPOINT: ${{ secrets.VITE_TELEMETRY_ENDPOINT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Initialize prompts DB
        run: npm run prompts:init

      - name: Sync prompts JSON
        run: npm run prompts:sync

      - name: Resolve adapter mode
        shell: bash
        run: |
          if [ -n "$VITE_LIVEBLOCKS_AUTH_ENDPOINT" ] || [ -n "$VITE_LIVEBLOCKS_PUBLIC_KEY" ]; then
            echo "VITE_ROOM_ADAPTER=liveblocks" >> "$GITHUB_ENV"
            echo "Using liveblocks adapter for this deploy."
          else
            echo "VITE_ROOM_ADAPTER=local" >> "$GITHUB_ENV"
            echo "Liveblocks config is missing. Falling back to local adapter for this deploy."
          fi

      - name: Build
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
